# Production Environment Variables for Railway
NODE_ENV=production
PORT=3000

# Railway Auto-assigns PORT, but this is the fallback
# RAILWAY_STATIC_URL will be available as your domain

# Scraping Configuration
MAX_CONCURRENT_REQUESTS=3
REQUEST_DELAY_MIN=3000
REQUEST_DELAY_MAX=8000
CAPTCHA_TIMEOUT=600000

# CORS Configuration for Railway domain
# Update this with your actual Railway domain after deployment
# CORS_ORIGIN=https://your-app-name.up.railway.app

# Optional: Custom domain (if you set up custom domain in Railway)
# DOMAIN=your-domain.com

# Railway-specific optimizations
# These are automatically set by Railway, but listed for reference:
# RAILWAY_ENVIRONMENT=production
# RAILWAY_PROJECT_ID=your-project-id
# RAILWAY_SERVICE_ID=your-service-id
